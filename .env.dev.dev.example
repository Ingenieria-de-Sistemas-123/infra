# Archivo de ejemplo para entorno DEV (no usar credenciales reales)
# NOTA: No subir el archivo .env.dev.dev real con credenciales al repo.

LANGUAGE_BASE_URL=http://language-service:8080
# Base URL para language-service (consumo interno entre contenedores)

SPRING_PROFILES_ACTIVE=docker
# Perfil Spring

SPRING_DATASOURCE_PASSWORD_PERMISSIONDB=${DB_PERMISSION_PASSWORD}
SPRING_DATASOURCE_USERNAME_PERMISSIONDB=${DB_PERMISSION_USER}
SPRING_DATASOURCE_URL_PERMISSIONDB=jdbc:postgresql://permission-db:5432/${DB_PERMISSION_NAME}

SPRING_DATASOURCE_PASSWORD_SNIPPET=${DB_PASSWORD}
SPRING_DATASOURCE_USERNAME_SNIPPET=${DB_USERNAME}
SPRING_DATASOURCE_URL_SNIPPET=jdbc:postgresql://snippet-db:5432/${DB_NAME}
# JDBC URLs (los servicios usan estos valores para conectarse internamente)

DB_PERMISSION_PASSWORD=CHANGEME_PERMISSION_DEV
DB_PERMISSION_USER=permission_user_dev
DB_PERMISSION_NAME=permission_dev
# --- Base de datos Permission ---

DB_PASSWORD=CHANGEME_SNIPPETS_DEV
DB_USERNAME=snippets_user_dev
DB_NAME=snippets_dev
# --- Base de datos Snippet ---

# Copiar a .env.dev.dev y reemplazar CHANGEME por valores reales

