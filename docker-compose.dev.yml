# yaml
name: infra-dev
services:
  permission-service:
    platform: linux/amd64
    image: ghcr.io/ingenieria-de-sistemas-123/permission-service:dev
    container_name: permission-service-dev
    expose:
      - "8080"
    environment:
      - SPRING_DATASOURCE_URL=${SPRING_DATASOURCE_URL_PERMISSIONDB}
      - SPRING_DATASOURCE_USERNAME=${SPRING_DATASOURCE_USERNAME_PERMISSIONDB}
      - SPRING_DATASOURCE_PASSWORD=${SPRING_DATASOURCE_PASSWORD_PERMISSIONDB}
      - SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI=${SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI}
      - AUTH0_AUDIENCE=${AUTH0_AUDIENCE}
      - ENVIRONMENT=${ENVIRONMENT:-dev}
      - NEW_RELIC_APP_NAME=SnippetSearcher-Permission
      - NEW_RELIC_LICENSE_KEY=${NEW_RELIC_LICENSE_KEY}
      - NEW_RELIC_LABELS=env:${ENVIRONMENT:-dev};service:permission-service;platform:snippet-searcher
      - NEW_RELIC_CONFIG_FILE=/newrelic/newrelic.yml
      - JAVA_TOOL_OPTIONS=${NEW_RELIC_JAVA_TOOL_OPTIONS--javaagent:/newrelic/newrelic.jar}
    volumes:
      - ./newrelic:/newrelic:ro
    depends_on:
      permission-db:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/actuator/health | grep UP || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 30
      start_period: 15s
    entrypoint: ["java"]
    command: ["-jar", "/app/app.jar"]

#PERMISSION DATABASE
  permission-db:
    image: postgres:16
    container_name: permission-db-dev
    environment:
      POSTGRES_DB: ${DB_PERMISSION_NAME}
      POSTGRES_USER: ${DB_PERMISSION_USER}
      POSTGRES_PASSWORD: ${DB_PERMISSION_PASSWORD}
    expose: ["5432"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -d $${POSTGRES_DB} -U $${POSTGRES_USER}"]
      interval: 5s
      timeout: 3s
      retries: 30
      start_period: 10s

  snippet-service:
    platform: linux/amd64
    image: ghcr.io/ingenieria-de-sistemas-123/snippet-service:dev
    container_name: snippet-service-dev
    expose: ["8080"]
    environment:
      - SPRING_DATASOURCE_URL=${SPRING_DATASOURCE_URL_SNIPPET}
      - SPRING_DATASOURCE_USERNAME=${SPRING_DATASOURCE_USERNAME_SNIPPET}
      - SPRING_DATASOURCE_PASSWORD=${SPRING_DATASOURCE_PASSWORD_SNIPPET}
      - SPRING_PROFILES_ACTIVE=docker
      - LANGUAGE_BASE_URL=http://language-service:8080
      - ASSET_SERVICE_BASE_URL=${ASSET_SERVICE_BASE_URL}
      - REDIS_HOST=${REDIS_HOST}
      - REDIS_PORT=${REDIS_PORT}
      - SPRING_DATA_REDIS_HOST=${SPRING_DATA_REDIS_HOST}
      - SPRING_DATA_REDIS_PORT=${SPRING_DATA_REDIS_PORT}
      - SPRING_DATA_REDIS_URL=${SPRING_DATA_REDIS_URL}
      - ENVIRONMENT=${ENVIRONMENT:-dev}
      - NEW_RELIC_APP_NAME=SnippetSearcher-Backend
      - NEW_RELIC_LICENSE_KEY=${NEW_RELIC_LICENSE_KEY}
      - NEW_RELIC_CONFIG_FILE=/newrelic/newrelic.yml
      - JAVA_TOOL_OPTIONS=${NEW_RELIC_JAVA_TOOL_OPTIONS--javaagent:/newrelic/newrelic.jar}
      - NEW_RELIC_LABELS=env:${ENVIRONMENT:-dev};service:snippet-service;platform:snippet-searcher

    volumes:
      - ./newrelic:/newrelic:ro
    depends_on:
      snippet-db:
        condition: service_healthy
      redis:
        condition: service_started
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/actuator/health | grep UP || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 30
      start_period: 15s

  asset-service:
    image: ghcr.io/austral-ingsis/snippet-asset-service:latest
    container_name: asset-service-dev
    expose: ["8080"]
    environment:
      AZURE_HOST: ${ASSET_SERVICE_AZURE_HOST}
      NEW_RELIC_APP_NAME: ${ASSET_SERVICE_NEW_RELIC_APP_NAME:-SnippetSearcher-Asset}
      NEW_RELIC_LICENSE_KEY: ${NEW_RELIC_LICENSE_KEY}
      NEW_RELIC_AGENT_ENABLED: ${ASSET_SERVICE_NEW_RELIC_AGENT_ENABLED:-true}
    depends_on:
      azurite:
        condition: service_started

  azurite:
    image: mcr.microsoft.com/azure-storage/azurite
    container_name: azurite-dev
    hostname: azurite
    restart: always
    ports:
      - "10000:10000"
      - "10001:10001"
      - "10002:10002"
    volumes:
      - azurite_blob_dev:/workspace

  snippet-db:
    image: postgres:16-alpine
    container_name: snippet-db-dev
    environment:
      POSTGRES_DB: ${DB_SNIPPET_NAME}
      POSTGRES_USER: ${DB_SNIPPET_USERNAME}
      POSTGRES_PASSWORD: ${DB_SNIPPET_PASSWORD}
    expose: ["5432"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -d $${POSTGRES_DB} -U $${POSTGRES_USER}"]
      interval: 5s
      timeout: 3s
      retries: 30
      start_period: 10s

  redis:
    image: redis:7
    container_name: redis-dev
    restart: always
    expose: ["6379"]

  language-service:
    platform: linux/amd64
    image: ghcr.io/ingenieria-de-sistemas-123/language-service:dev
    container_name: language-service-dev
    expose: ["8080"]
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - ENVIRONMENT=${ENVIRONMENT:-dev}
      - NEW_RELIC_APP_NAME=SnippetSearcher-Language
      - NEW_RELIC_LICENSE_KEY=${NEW_RELIC_LICENSE_KEY}
      - NEW_RELIC_CONFIG_FILE=/newrelic/newrelic.yml
      - JAVA_TOOL_OPTIONS=${NEW_RELIC_JAVA_TOOL_OPTIONS--javaagent:/newrelic/newrelic.jar}
      - NEW_RELIC_LABELS=env:${ENVIRONMENT:-dev};service:language-service;platform:snippet-searcher
    volumes:
      - ./newrelic:/newrelic:ro
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/actuator/health | grep UP || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 30
      start_period: 15s
    entrypoint: ["java"]
    command: ["-jar", "/app/spring-boot-application.jar"]

  printscript-ui:
    platform: linux/amd64
    container_name: "printscript-ui-dev"
    image: ghcr.io/ingenieria-de-sistemas-123/printscript-ui:dev
    env_file: [.env.dev]
    expose:
      - "80"

  reverse-proxy-dev:
    image: nginx:alpine
    container_name: reverse-proxy-dev
    depends_on:
      permission-service:
        condition: service_healthy
      snippet-service:
        condition: service_healthy
      language-service:
        condition: service_healthy
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d/dev.conf:/etc/nginx/conf.d/default.conf:ro
      - certbot_www_dev:/var/www/certbot
      - letsencrypt_dev:/etc/letsencrypt

  certbot:
    platform: linux/amd64
    image: certbot/certbot:latest
    container_name: certbot-dev
    depends_on:
      - reverse-proxy-dev
    volumes:
      - certbot_www_dev:/var/www/certbot
      - letsencrypt_dev:/etc/letsencrypt
    entrypoint: /bin/sh
    command: -c "trap exit TERM; while :; do sleep 12h & wait $!; certbot renew --webroot -w /var/www/certbot --quiet; done"

networks:
  default:
    driver: bridge

volumes:
  certbot_www_dev:
  letsencrypt_dev:
  azurite_blob_dev:
